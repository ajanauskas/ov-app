<nav class="navbar navbar-inverse navbar-static-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#main-navigation-bar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <div class="navbar-collapse collapse" id="main-navigation-bar">
      <ul class="nav navbar-nav">
        <li>
          <%= link_to I18n.t('welcome.index'), root_path %>
        </li>
        <% if @current_user.nil? %>
          <li>
            <%= link_to I18n.t('login.login'), login_users_path %>
          </li>
          <li>
            <%= link_to I18n.t('users.signup'), new_user_path %>
          </li>
        <% else %>
          <li>
            <%= link_to @current_user.login, user_path(@current_user) %>
          </li>
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
              <%= t('games.title') %> <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to t('me.games.title'), my_games_path %>
              </li>
              <li>
                <%= link_to t('me.games.new'), new_my_game_path %>
              </li>
            </ul>
          </li>
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
              <%= t('teams.index.title') %> <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to t('teams.index.title'), teams_path %>
              </li>
              <li>
                <%= link_to t('me.teams.new.title'), new_my_team_path %>
              </li>
            </ul>
          </li>
        <% end %>
        <li>
          <%= link_to t('users.title'), users_path %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if @current_user %>
          <li>
            <%= link_to I18n.t('users.logout'), logout_users_path, data: { method: :put } %>
          </li>
        <% end %>
        <%= render 'layouts/change_locale_dropdown' %>
      </ul>
    </div>
  </div>
</nav>

